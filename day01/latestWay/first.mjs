import sum from "./second.mjs"
sum(2,5);

console.log("first")

// for import and export we use the file_name.mjs 
// if we don't want to use .mjs so we can create a package.json with the given code in that